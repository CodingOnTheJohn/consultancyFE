<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-xs-12 col-sm-12 col-md-10 col-lg-8 col-xl-8">
      <h2 class="mb-4">Ruby Lessons</h2>

      <div class="list-group">
        <% @ruby_lessons.each do |lesson| %>
          <%= link_to ruby_lesson_path(id: lesson.id), class: "list-group-item list-group-item-action d-flex justify-content-between align-items-center" do %>
            <span>Lesson <%= lesson.id %></span>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
