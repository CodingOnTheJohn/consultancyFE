<div class="container">
  <div class="col-xs-12 col-sm-12 col-md-11 col-lg-10 col-xl-10">
    <h4 class="mt-3"> Interview Lessons </h4>
    <% @interview_lessons.each do |lesson| %>
      <ul>
        <li><%= link_to "#{lesson.id}", interview_lesson_path(id: lesson.id) %></li>
      </ul>
    <% end %>
  </div>
</div>